- Think some more about type mapping. Is it always possible to have
  a one-to-one map? If not, what can we assume?

- Fix every FIXME

- Fix initialization of declared variables. Currently they're ignored
  by the translator.

- Pretty print continued if-then-else's so we don't get a huge
  indent. Test 16 is an example of this

- Be smart with parenthesis
